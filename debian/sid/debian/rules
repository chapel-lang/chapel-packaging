#!/usr/bin/make -f
# -*- makefile -*-

export DH_VERBOSE=1
export PYTHONDONTWRITEBYTECODE=1
export CHPL_HOME=../

%:
	dh $@ --with python2

override_dh_auto_test:


override_dh_auto_clean:
	dh_auto_clean -- clobber
